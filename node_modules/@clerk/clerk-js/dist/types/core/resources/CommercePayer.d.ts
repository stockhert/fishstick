import type { CommercePayerJSON, CommercePayerResource } from '@clerk/types';
import { BaseResource } from './internal';
export declare class CommercePayer extends BaseResource implements CommercePayerResource {
    id: string;
    createdAt: Date;
    updatedAt: Date;
    imageUrl: string | null;
    userId?: string;
    email?: string;
    firstName?: string;
    lastName?: string;
    organizationId?: string;
    organizationName?: string;
    constructor(data: CommercePayerJSON);
    protected fromJSON(data: CommercePayerJSON | null): this;
}
